/** @type {import('next').NextConfig} */
const nextConfig = {
    // Ensure static output is not strictly required unless using 'export'
    // output: 'export', // Uncomment if we wanted static export, but Vercel handles default better
};

export default nextConfig;
